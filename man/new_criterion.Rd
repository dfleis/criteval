% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{new_criterion}
\alias{new_criterion}
\title{Create a criterion definition object}
\usage{
new_criterion(name, precompute = NULL, compute)
}
\arguments{
\item{name}{A character string, the name of the criterion.}

\item{precompute}{A function that takes the full simulation data list (\code{sim.data})
and performs any necessary calculations on the parent node before the split
loop begins. It should return a list or object containing pre-computed values.
If no pre-computation is needed, this can be \code{NULL}.}

\item{compute}{A function that calculates the criterion's value for a single split.
It must accept two arguments:
\describe{
\item{\code{split.data}}{A list containing data for the current split, such as
child node estimates (\code{theta.hat.C1}, \code{theta.hat.C2}), population
parameters (\code{theta.star.C1}, \code{theta.star.C2}), the weight (\code{wt}), and the
Jacobian/covariance matrix (\code{V}).}
\item{\code{precomputed.data}}{The object returned by the \code{precompute} function.}
}}
}
\value{
An object of class \code{criteval_criterion}, which is a list containing the
\code{name}, \code{precompute}, and \code{compute} functions.
}
\description{
This constructor function creates a standardized object that defines a splitting
criterion. This allows the main simulation loop to be agnostic to the specific
calculations of any given criterion.
}
\keyword{internal}
